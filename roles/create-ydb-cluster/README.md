# Плейбук разворачивает кластер YDB
## Для запуска
Вносим изменения ip-адресов в hosts.yml. Название группы ydb_cluster не меняем, от этого зависит логика задач
### Выполняем команду
```
ansible-playbook playbook-ydb-cluster.yml
```
